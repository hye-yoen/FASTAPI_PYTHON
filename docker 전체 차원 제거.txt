docker 전체 차원 제거
docker system prune -a --volumes



Dockerfile 
-> docker-compose.yml: 
	volumes(model.pkl: 모델 파일 마운트 (읽기 전용) ,main.py :개발 모드: 코드 수정 시 반영 ),
	    healthcheck
-> main.py : # CORS 설정 (프론트엔드에서 접근 가능하도록)# 헬스 체크 엔드포인트
-> MLTestContorller : 
	private final String FASTAPI_BASE_URL = "http://localhost:8000"; // FastAPI 서버 주소

//==============================
Springboot          			KAFKA			FASTAPI			REDIS
FN/BN(KAFKA 로 정보보내기)	 -> 	spring 저장 및 <- 	데이터 처리 후	<->  Spring Boot, FASTAPI가 자주 사용하는 데이터를 						fastapi로 전달		redis로 전송			빠르게 읽고 쓸 수 있도록 지원









docker network lm 
docker network rm (docker network 이름)
docker network create ( "" )
